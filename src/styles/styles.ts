import styled from 'styled-components';

export const Header = styled.div`
  display: flex;
  /* background: url('../../public/images/logo-space-y.svg'); */
  /* background-color: var(--space); */
  font: var(--font-display);
`;
